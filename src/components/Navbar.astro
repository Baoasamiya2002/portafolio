---
import {
  getLangFromUrl,
  useTranslations,
  useTranslatedPath,
} from "../i18n/utils";
import type { NavigationTypeString } from "astro:transitions/client";
import { ThemeSwitch } from "free-astro-components";
import { Icon } from "astro-icon/components";
import { DarkMode } from "accessible-astro-components";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

interface NavItem {
  name: string;
  href: string;
}
const navItems: NavItem[] = [
  { name: t("nav.welcome"), href: translatePath("/") },
  { name: t("nav.prototype"), href: translatePath("/web-proto") },
  { name: t("nav.evaluation"), href: translatePath("/eva-and-redes-works") },
  { name: t("nav.videogame"), href: translatePath("/videogame/") },
];
---

<div>
  <div class="bg-figure pentagon_menu w-11 p-1.5 absolute">
    <ul>
      <li class="h-13 flex">
        <!--Icon
          id="menu-btn"
          icon="menu"
          class="flex icon-class cursor-pointer"
        /!-->
      </li>
      <li
        class="bg-bg hover:bg-black darkmode:hover:bg-white hexagon_menu transition duration-200"
      >
        <DarkMode
          initialMode="auto"
          class="border-0! px-1! py-1! pbs-3! shadow-none! hover:text-white darkmode:hover:text-black"
        >
          <Icon name="moon-stars" slot="dark" />
          <Icon name="sun" slot="light" />
        </DarkMode>
      </li>
      <li class="bg-bg hover:bg-black darkmode:hover:bg-white hexagon_menu">
        <a
          id="lang_es_btn"
          class="flex"
          href={translatePath("/", t("btn.nextLang"))}
          >{t("btn.nextLang").toUpperCase()}</a
        >
      </li>
    </ul>
  </div>
  <div id="menu-modal" class="bg-white/50 fixed inset-0 hidden z-10">
    <div id="menu" class="w-md bg-indigo-800 pentagon p-2 absolute">
      <div class="bg-white pentagon left-0 h-100 flex justify-center pt-10">
        <nav class="inline-flex">
          <ul class="flex flex-col gap-6">
            {
              navItems.map((item: NavItem) => (
                <li>
                  <a
                    class="hover:bg-indigo-800 hover:text-white pentagon_item text-2xl text-center flex items-center p-2 w-[24.3rem]"
                    href={item.href}
                  >
                    {item.name}
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>
      </div>
    </div>
  </div>
</div>

<script>
  const menu_btn = document.getElementById("menu-btn");
  const menu_modal = document.getElementById("menu-modal");
  const menu = document.getElementById("menu");

  menu?.addEventListener("mouseleave", () => {
    menu_modal?.classList.add("hidden");
  });

  function openMenu() {
    menu_modal?.classList.remove("hidden");
  }

  menu_btn?.addEventListener("click", openMenu);
  menu_btn?.addEventListener("mouseover", openMenu);
</script>
